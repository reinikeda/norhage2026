name: Deploy Theme and Custom Plugins (Staging)

on:
  push:
    branches: [staging]
    paths:
      - 'public/wp-content/themes/astra-custom-for-norhage/**'
      - 'public/wp-content/plugins/**'

concurrency:
  group: deploy-staging
  cancel-in-progress: true

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      theme_changed: ${{ steps.filter.outputs.theme }}
      plugins_json: ${{ steps.setmatrix.outputs.plugins_json }}
      plugins_changed: ${{ steps.setmatrix.outputs.plugins_changed }}
    steps:
      - uses: actions/checkout@v4

      - name: Detect changed paths
      # This action tells you which "filters" matched the changed files
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            theme:
              - 'public/wp-content/themes/astra-custom-for-norhage/**'
            nh-cutting-toggle:
              - 'public/wp-content/plugins/nh-cutting-toggle/**'
            nh-delivery-time:
              - 'public/wp-content/plugins/nh-delivery-time/**'
            nh-faq:
              - 'public/wp-content/plugins/nh-faq/**'
            nh-feature-box:
              - 'public/wp-content/plugins/nh-feature-box/**'
            nh-filters:
              - 'public/wp-content/plugins/nh-filters/**'
            nh-home-builder:
              - 'public/wp-content/plugins/nh-home-builder/**'
            nh-important-notes:
              - 'public/wp-content/plugins/nh-important-notes/**'
            nh-inactivity-popup-lite:
              - 'public/wp-content/plugins/nh-inactivity-popup-lite/**'
            nh-sale-slider:
              - 'public/wp-content/plugins/nh-sale-slider/**'
            nh-shipping-calculator:
              - 'public/wp-content/plugins/nh-shipping-calculator/**'
            product-labels:
              - 'public/wp-content/plugins/product-labels/**'
            running-ticker-line:
              - 'public/wp-content/plugins/running-ticker-line/**'

      - name: Build plugins matrix JSON (only changed)
        id: setmatrix
        shell: bash
        run: |
          changed=()

          # Add plugin slugs here (same as filters above)
          plugins=(
            nh-cutting-toggle
            nh-delivery-time
            nh-faq
            nh-feature-box
            nh-filters
            nh-home-builder
            nh-important-notes
            nh-inactivity-popup-lite
            nh-sale-slider
            nh-shipping-calculator
            product-labels
            running-ticker-line
          )

          for p in "${plugins[@]}"; do
            val="${{ steps.filter.outputs[format('{0}', 'REPLACE_ME')] }}"
          done

          # Bash can't directly index that expression, so we do it with a case map:
          for p in "${plugins[@]}"; do
            case "$p" in
              nh-cutting-toggle)          out="${{ steps.filter.outputs.nh-cutting-toggle }}" ;;
              nh-delivery-time)           out="${{ steps.filter.outputs.nh-delivery-time }}" ;;
              nh-faq)                     out="${{ steps.filter.outputs.nh-faq }}" ;;
              nh-feature-box)             out="${{ steps.filter.outputs.nh-feature-box }}" ;;
              nh-filters)                 out="${{ steps.filter.outputs.nh-filters }}" ;;
              nh-home-builder)            out="${{ steps.filter.outputs.nh-home-builder }}" ;;
              nh-important-notes)         out="${{ steps.filter.outputs.nh-important-notes }}" ;;
              nh-inactivity-popup-lite)   out="${{ steps.filter.outputs.nh-inactivity-popup-lite }}" ;;
              nh-sale-slider)             out="${{ steps.filter.outputs.nh-sale-slider }}" ;;
              nh-shipping-calculator)     out="${{ steps.filter.outputs.nh-shipping-calculator }}" ;;
              product-labels)             out="${{ steps.filter.outputs.product-labels }}" ;;
              running-ticker-line)        out="${{ steps.filter.outputs.running-ticker-line }}" ;;
              *) out="false" ;;
            esac

            if [ "$out" = "true" ]; then
              changed+=("\"$p\"")
            fi
          done

          if [ ${#changed[@]} -eq 0 ]; then
            echo "plugins_changed=false" >> $GITHUB_OUTPUT
            echo 'plugins_json=[]' >> $GITHUB_OUTPUT
          else
            echo "plugins_changed=true" >> $GITHUB_OUTPUT
            echo "plugins_json=[${changed[*]}]" >> $GITHUB_OUTPUT
          fi

  deploy-theme-staging:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.theme_changed == 'true'

    strategy:
      max-parallel: 1
      matrix:
        site:
          - /staging
          - /staging-fi
          - /staging-de
          - /staging-se
          - /staging-no

    steps:
      - uses: actions/checkout@v4

      - name: Deploy theme to ${{ matrix.site }} via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.6
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          port: 22
          sftp_only: true
          local_path: "public/wp-content/themes/astra-custom-for-norhage/*"
          remote_path: "${{ matrix.site }}/wp-content/themes/astra-custom-for-norhage"

  deploy-plugins-staging:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.plugins_changed == 'true'

    strategy:
      max-parallel: 1
      matrix:
        site:
          - /staging
          - /staging-fi
          - /staging-de
          - /staging-se
          - /staging-no
        plugin: ${{ fromJSON(needs.detect-changes.outputs.plugins_json) }}

    steps:
      - uses: actions/checkout@v4

      - name: Deploy plugin ${{ matrix.plugin }} to ${{ matrix.site }} via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.6
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          port: 22
          sftp_only: true
          local_path: "public/wp-content/plugins/${{ matrix.plugin }}/*"
          remote_path: "${{ matrix.site }}/wp-content/plugins/${{ matrix.plugin }}"